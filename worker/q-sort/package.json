{
  "name": "q-sort-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker for Q-sort live tally storage. Used by /phones-q-sort/ on foxxelabs.ie.",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.78.0"
  }
}
